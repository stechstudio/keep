{
    "name": "stechstudio/keep",
    "description": "Standalone secret management tool for any PHP project with support for AWS SSM, Secrets Manager, and more.",
    "keywords": [
        "secrets",
        "secret-management",
        "aws-ssm",
        "secrets-manager",
        "environment",
        "cli",
        "devops"
    ],
    "license": "MIT",
    "authors": [
        {
            "name": "Joseph Szobody",
            "email": "joseph@stechstudio.com",
            "role": "Developer"
        }
    ],
    "require": {
        "php": "^8.3",
        "illuminate/support": "^10.0||^11.0||^12.0",
        "laravel/prompts": "^0.1.0||^0.2.0||^0.3.0",
        "symfony/console": "^6.0||^7.0"
    },
    "require-dev": {
        "aws/aws-sdk-php": "^3.354",
        "mockery/mockery": "^1.5",
        "phpstan/extension-installer": "^1.3",
        "phpstan/phpstan": "^1.10",
        "phpstan/phpstan-phpunit": "^1.3||^2.0",
        "phpunit/phpunit": "^10.0||^11.0",
        "symfony/var-dumper": "^7.3"
    },
    "suggest": {
        "aws/aws-sdk-php": "Required to use AWS SSM and Secrets Manager vault drivers."
    },
    "autoload": {
        "psr-4": {
            "STS\\Keep\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "STS\\Keep\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "analyse": "vendor/bin/phpstan analyse",
        "test": "vendor/bin/phpunit",
        "test-coverage": "vendor/bin/phpunit --coverage-text",
        "lint": [
            "@php vendor/bin/phpstan analyse --verbose --ansi"
        ]
    },
    "bin": ["bin/keep"],
    "config": {
        "sort-packages": true,
        "allow-plugins": {
            "phpstan/extension-installer": true
        }
    },
    "minimum-stability": "dev",
    "prefer-stable": true
}
